{
  "name": "projectmath",
  "private": true,
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "node dev.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
