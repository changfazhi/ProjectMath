node_modules
**/node_modules
**/dist
.git
**/.env
**/.env.*
!**/.env.example
# Frontend Firebase config is public client config and must be present at Vite
# build time (baked into the bundle), so allow just these into the build.
# .env.production is the committed copy CI builds use; .env is the local one.
!frontend/.env
!frontend/.env.production
.planning
.claude
2025
images
*.pdf
dev.js
