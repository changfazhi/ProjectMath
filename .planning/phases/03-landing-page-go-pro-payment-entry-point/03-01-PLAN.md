---
phase: 03-landing-page-go-pro-payment-entry-point
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/LandingPage.tsx
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03]

must_haves:
  truths:
    - Clicking "Go Pro" in the landing page Pricing section triggers the upgrade flow instead of scrolling to the top / doing nothing (PAY-01)
    - A logged-in visitor clicking "Go Pro" sees the UpgradeModal open on the landing page with no navigation or redirect (PAY-03)
    - A logged-out visitor clicking "Go Pro" sees the LoginModal with the "Log in to continue to Premium checkout." message; after a successful sign-in the UpgradeModal opens automatically and the visitor stays on the landing page (PAY-02)
    - A plain nav "Log in" sign-in still redirects to /roadmap (replace) and does NOT open the UpgradeModal (Success Criterion 4 — non-regression guard)
  artifacts:
    - frontend/src/pages/LandingPage.tsx (modified — data-goto-pro attribute, goProIntentRef, openUpgradeModal destructure, handleClick branch, transition-effect branch)
  key_links:
    - data-goto-pro attribute (HTML) resolves to anchor.dataset.gotoPro (JS camelCase) — a name mismatch silently disables the whole CTA
    - goProIntentRef set in handleClick is read in the justLoggedIn transition effect — the auto-open path depends on the ref surviving between the click and the onAuthStateChanged transition (it does; the component does not unmount during the LoginModal flow)
    - goProIntentRef is cleared once consumed in the transition effect — prevents a stale "Go Pro" intent leaking into a later plain sign-in
---

<objective>
Wire the landing page Pricing "Go Pro" CTA (currently a dead `href="#"` link) to the already-shipped upgrade flow, entirely within `frontend/src/pages/LandingPage.tsx`, reusing the globally-mounted `UpgradeModal`/`openUpgradeModal()` and the existing `LoginModal`/`openLoginModal(message?)` — no new components, no new backend routes, no changes to `AuthContext`, `Header`, `LoginModal`, or `UpgradeModal`.

Purpose: Turn the landing page's pricing pitch into a working payment entry point so visitors can start a Premium upgrade without first finding their way into the app.
Output: A modified `LandingPage.tsx` that (a) delegates the "Go Pro" click through `handleClick()`, (b) opens the upgrade modal instantly for logged-in visitors, and (c) for logged-out visitors opens the login modal first and then auto-opens the upgrade modal on successful sign-in, while leaving the plain-login → `/roadmap` redirect untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-landing-page-go-pro-payment-entry-point/03-UI-SPEC.md

@frontend/src/pages/LandingPage.tsx
@frontend/src/contexts/AuthContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delegate the "Go Pro" click — instant upgrade for logged-in, login-first for logged-out</name>
  <files>frontend/src/pages/LandingPage.tsx</files>
  <read_first>
    - frontend/src/pages/LandingPage.tsx (the whole file — the static `MARKUP` string, the `handleClick()` delegation at lines 495-512, the `useAuth()` destructure at line 477, and the "Go Pro" `<a>` at line 402)
    - frontend/src/contexts/AuthContext.tsx (confirm the `openLoginModal(message?)` signature at line 71 and `openUpgradeModal()` at line 75 exposed by `useAuth()`)
    - .planning/phases/03-landing-page-go-pro-payment-entry-point/03-UI-SPEC.md ("Interaction Contract" sections 1, 2, 3 — the binding spec for this phase)
  </read_first>
  <action>
    In `frontend/src/pages/LandingPage.tsx`:

    1. Add `openUpgradeModal` to the `useAuth()` destructure (currently `const { user, loading, openLoginModal } = useAuth()`) so the component can open the upgrade modal directly.

    2. Add a component-scoped ref `goProIntentRef` initialised to `false` via `useRef(false)` (`useRef` is already imported), placed alongside the existing `prevUserRef`. This flag marks "the login now in progress was triggered by Go Pro." Do NOT persist it to localStorage/sessionStorage/cookies — it must be a plain in-memory React ref so it resets on reload (see threat T-03-01).

    3. In the static `MARKUP` string, add a bare `data-goto-pro` attribute to the Pro card's "Go Pro" anchor (line 402: `<a href="#" class="pm-hov-lift" style="...">Go Pro</a>`). Keep `href="#"`, the `pm-hov-lift` class, and the entire inline `style` byte-for-byte unchanged — only insert the attribute. Do NOT convert the `<a>` to a `<button>` and do NOT rename the "Go Pro" copy (per UI-SPEC Copywriting Contract and Interaction Contract section 1).

    4. In `handleClick()`, add a new branch keyed on `anchor.dataset.gotoPro !== undefined` (the HTML `data-goto-pro` maps to the JS `dataset.gotoPro`). In that branch: call `e.preventDefault()` (so the dead `#` scroll-to-top never fires), then branch on auth state — if `user` is truthy, call `openUpgradeModal()` (logged-in → instant open, mirroring the Header "Get Premium" button, per Interaction Contract section 2); otherwise set `goProIntentRef.current = true` and call `openLoginModal('Log in to continue to Premium checkout.')` (logged-out → login-first with the tailored message, per Interaction Contract section 3 and the Copywriting Contract). Place this branch after the existing `data-login` branch and before the generic `#`-scroll branch.

    Do not touch the `data-login`, `href === '/roadmap'`, or `#`-scroll branches. Do not modify `AuthContext.tsx`, `LoginModal.tsx`, `UpgradeModal.tsx`, or `Header.tsx`.
  </action>
  <acceptance_criteria>
    - Source assertion: `frontend/src/pages/LandingPage.tsx` contains the string `data-goto-pro` on the "Go Pro" anchor within the `MARKUP` template, and the anchor still contains `href="#"`, `class="pm-hov-lift"`, and its original inline gradient `style` (unchanged).
    - Source assertion: `handleClick()` contains a branch testing `anchor.dataset.gotoPro !== undefined` that calls `e.preventDefault()`, calls `openUpgradeModal()` when `user` is truthy, and calls `openLoginModal('Log in to continue to Premium checkout.')` (setting `goProIntentRef.current = true`) when `user` is falsy.
    - Source assertion: the `useAuth()` destructure includes `openUpgradeModal`, and a `goProIntentRef` is declared via `useRef(false)`.
    - Behavior assertion (typecheck): `cd frontend && npm run build` completes with no TypeScript errors.
    - The "Go Pro" label text is unchanged (still literally "Go Pro"); no new component file is created.
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'data-goto-pro' frontend/src/pages/LandingPage.tsx && grep -n 'dataset.gotoPro' frontend/src/pages/LandingPage.tsx && grep -n "openLoginModal('Log in to continue to Premium checkout.')" frontend/src/pages/LandingPage.tsx && grep -n 'goProIntentRef' frontend/src/pages/LandingPage.tsx</automated>
  </verify>
  <done>
    Grep confirms the `data-goto-pro` attribute, the `dataset.gotoPro` handleClick branch, the tailored login message, and `goProIntentRef`. `cd frontend && npm run build` passes. PAY-01 (CTA is no longer a dead link) and PAY-03 (logged-in → instant UpgradeModal) are satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resume the Go Pro intent after login — auto-open upgrade, guard the plain-login redirect</name>
  <files>frontend/src/pages/LandingPage.tsx</files>
  <read_first>
    - frontend/src/pages/LandingPage.tsx (the `justLoggedIn` transition effect at lines 485-489, and `goProIntentRef` added in Task 1)
    - .planning/phases/03-landing-page-go-pro-payment-entry-point/03-UI-SPEC.md ("Interaction Contract" section 3 steps 4-5 and section 4 — the login→upgrade sequencing and the "no new transition affordance" rule)
    - .planning/ROADMAP.md (Phase 3 Success Criterion 4 — the plain-login non-regression guard)
  </read_first>
  <action>
    In `frontend/src/pages/LandingPage.tsx`, modify the existing `justLoggedIn` transition effect (the `useEffect` at lines 485-489 that currently always calls `navigate('/roadmap', { replace: true })` on a fresh sign-in).

    Inside the `if (!loading && justLoggedIn)` block, branch on `goProIntentRef.current`:
    - If `goProIntentRef.current` is true: set `goProIntentRef.current = false` first (consume/clear the flag so it cannot leak into a later plain sign-in — see threat T-03-01), then call `openUpgradeModal()` and do NOT navigate. The visitor stays on the landing page with the UpgradeModal open over it (Interaction Contract section 3 step 4a).
    - Otherwise: keep the existing behavior exactly — `navigate('/roadmap', { replace: true })` (Interaction Contract section 3 step 4b; this is the Success Criterion 4 regression guard for a plain nav "Log in" sign-in).

    Add `openUpgradeModal` to the effect's dependency array to satisfy react-hooks/exhaustive-deps. Re-running the effect is safe: after the first run `prevUserRef.current === user`, so `justLoggedIn` is false and the branch is a no-op.

    Do NOT add any spinner, skeleton, toast, redirect flash, or global loading overlay for the gap between LoginModal closing and UpgradeModal opening — per Interaction Contract section 4, the brief sub-second interval is left as a plain view of the landing page (matching the Header's existing direct-open behavior). No new visual surface.
  </action>
  <acceptance_criteria>
    - Source assertion: the `justLoggedIn` transition effect branches on `goProIntentRef.current`; the true branch clears the ref (`goProIntentRef.current = false`) and calls `openUpgradeModal()` without calling `navigate`; the false branch calls `navigate('/roadmap', { replace: true })`.
    - Source assertion: `openUpgradeModal` appears in the transition effect's dependency array.
    - Source assertion (no-regression): the string `navigate('/roadmap', { replace: true })` still appears in the transition effect (plain-login path preserved).
    - Source assertion (no new affordance): no new spinner/skeleton/toast/overlay markup or component is introduced for the modal-to-modal transition.
    - Behavior assertion (typecheck): `cd frontend && npm run build` completes with no TypeScript errors.
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'goProIntentRef.current' frontend/src/pages/LandingPage.tsx && grep -n "navigate('/roadmap', { replace: true })" frontend/src/pages/LandingPage.tsx && (cd frontend && npm run build)</automated>
  </verify>
  <done>
    The transition effect auto-opens the UpgradeModal only when `goProIntentRef.current` is set (then clears it), and otherwise preserves the plain-login → `/roadmap` redirect. `cd frontend && npm run build` passes. PAY-02 (logged-out → login → auto-upgrade) is satisfied and Success Criterion 4 (plain login unaffected) is guarded.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created/added by this phase (for downstream verification and cross-referencing):

- `data-goto-pro` — new bare data attribute on the Pricing "Go Pro" `<a>` in the `MARKUP` string of `LandingPage.tsx`
- `anchor.dataset.gotoPro` — new branch key inside `handleClick()` in `LandingPage.tsx`
- `goProIntentRef` — new component-scoped React ref (`useRef(false)`) in `LandingPage.tsx` marking "this login is a Go Pro intent"
- `openUpgradeModal` — newly destructured from `useAuth()` inside `LandingPage.tsx` (the function itself already exists in `AuthContext.tsx`; this phase adds its consumption here)
- Modified `justLoggedIn` transition `useEffect` in `LandingPage.tsx` — now branches on `goProIntentRef.current` (auto-open upgrade) vs. the existing `/roadmap` redirect
- Login prompt copy string: `"Log in to continue to Premium checkout."` (passed into the existing `openLoginModal(message?)` API; reuses `LoginModal`'s existing `message` prop — no new copy field)

No new files, components, endpoints, dependencies, or exported symbols are created.
</artifacts_this_phase_produces>

<multi_source_coverage_audit>
Every source item is COVERED by a plan. No unplanned items; no phase split required.

**GOAL (ROADMAP Phase 3 goal)** — "Visitors can start a Premium upgrade directly from the landing page's Pricing section; logged-in → upgrade modal instantly; logged-out → login then upgrade modal automatically."
- COVERED: Plan 03-01 Task 1 (instant open for logged-in; login-first for logged-out) + Task 2 (auto-open after login). Verified end-to-end by Plan 03-02.

**REQ (phase_req_ids)**
- PAY-01 (CTA opens real upgrade flow, not a dead `href="#"`) — COVERED: Plan 03-01 Task 1.
- PAY-02 (logged-out → login → auto-land in upgrade modal) — COVERED: Plan 03-01 Task 1 (sets intent + opens login) and Task 2 (resumes intent → opens upgrade).
- PAY-03 (logged-in → upgrade modal immediately) — COVERED: Plan 03-01 Task 1.

**RESEARCH (RESEARCH.md)** — none exists; intentionally skipped for this small, codebase-already-explored milestone (per planning context). Not a gap.

**CONTEXT (decisions — PROJECT.md "Current Milestone: v1.1" as the CONTEXT.md substitute; no formal D-NN IDs exist for this phase)**
- Decision: "Reuse the existing globally-mounted `UpgradeModal`/`openUpgradeModal()` rather than a landing-page-specific modal." — HONORED: Plan 03-01 uses `openUpgradeModal()`; no new modal created.
- Decision: "Keep the Header 'Get Premium' button unchanged (second, unmodified entry point)." — HONORED by omission: `Header.tsx` is not in `files_modified` and is explicitly out of scope.
- Decision: "Frontend-only wiring; no new backend routes (`POST /api/billing/checkout` and the upgrade modal already exist)." — HONORED: `files_modified` is `LandingPage.tsx` only; no backend files touched.
- Decision/Success Criterion 4: "Plain sign-in still redirects to `/roadmap` and does NOT open the upgrade modal." — HONORED: Plan 03-01 Task 2 branch + Plan 03-02 checkpoint session-leak test.

**Exclusions (not gaps):** "Going live with real Stripe payments / production deployment" and "changing the Header 'Get Premium' button" are explicitly out of scope for this phase (ROADMAP + REQUIREMENTS Out of Scope).
</multi_source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser React state ↔ Firebase Auth | `onAuthStateChanged` drives the logged-out→logged-in transition consumed by this phase's effect. Client-only; no untrusted input is authored here. |
| browser ↔ backend `POST /api/billing/checkout` | Pre-existing, unchanged boundary. Invoked only by the reused `UpgradeModal`; this phase does not modify the request, its payload, or its auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege (client logic) | `goProIntentRef` in `LandingPage.tsx` | low | mitigate | Clear `goProIntentRef.current = false` in the true-branch of the `justLoggedIn` effect the moment it is consumed (Task 2). The flag is a plain in-memory React ref — never written to localStorage/sessionStorage/cookies — so it resets on reload/unmount, preventing a stale "Go Pro" intent from leaking into a later unrelated sign-in. Verified by the Plan 03-02 session-leak checkpoint. |
| T-03-02 | Spoofing (wrong-user intent) | login→upgrade sequencing in `LandingPage.tsx` | low | accept | The intent is read only on the logged-out→logged-in transition and immediately cleared. `openUpgradeModal()` merely opens a client-side modal; the actual checkout (`POST /api/billing/checkout`, unchanged) is authenticated server-side by whichever user is signed in, so opening the modal is non-destructive regardless of who completes the login. |
| T-03-03 | Denial of Service / Information Disclosure | `POST /api/billing/checkout` | low | accept | No new endpoint, data flow, or attack surface. This phase reuses the existing, already-reviewed and rate-limited checkout route unchanged. |
| T-03-SC | Tampering (supply chain) | npm installs | low | accept | No packages are installed in this phase. The UI-SPEC forbids new dependencies ("do not initialize shadcn or add new dependencies for this work"); `package.json` is not modified. No install step exists, so the legitimacy/slopcheck gate is not applicable; if an install is later introduced it must route through the Package Legitimacy Gate. |

All threats are low severity; none meets the "block on high severity" threshold. No high/critical threats introduced (this phase only rewires which client-side handler fires an already-reviewed flow).
</threat_model>

<verification>
- `cd frontend && npm run build` passes (TypeScript typecheck + Vite build) — no type errors introduced.
- `cd frontend && npm run lint` passes (no new ESLint errors, incl. react-hooks/exhaustive-deps for the modified effect).
- Grep confirms all source assertions: `data-goto-pro`, `dataset.gotoPro`, the tailored login message, `goProIntentRef`, the preserved `navigate('/roadmap', { replace: true })`, and `openUpgradeModal` in both the `useAuth()` destructure and the effect deps.
- No changes outside `frontend/src/pages/LandingPage.tsx` (`git diff --name-only` lists only that file).
- Runtime behavior (three flows + non-regression) is gated by the human-verify checkpoint in Plan 03-02.
</verification>

<success_criteria>
- The Pricing "Go Pro" CTA no longer behaves as a dead `href="#"` link (PAY-01).
- A logged-in visitor clicking "Go Pro" opens the `UpgradeModal` immediately with no navigation (PAY-03).
- A logged-out visitor clicking "Go Pro" opens `LoginModal` (with the Premium message) and, after sign-in, the `UpgradeModal` opens automatically while staying on the landing page (PAY-02).
- A plain nav "Log in" sign-in still redirects to `/roadmap` and does not open the `UpgradeModal` (Success Criterion 4).
- Only `LandingPage.tsx` is modified; no new components/routes/dependencies; `UpgradeModal`, `LoginModal`, `AuthContext`, and `Header` are unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-landing-page-go-pro-payment-entry-point/03-01-SUMMARY.md` when done.
</output>
