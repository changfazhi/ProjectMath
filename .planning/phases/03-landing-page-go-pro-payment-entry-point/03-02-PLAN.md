---
phase: 03-landing-page-go-pro-payment-entry-point
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: false
requirements: [PAY-01, PAY-02, PAY-03]

must_haves:
  truths:
    - The three "Go Pro" flows behave as specified at runtime, and a plain sign-in does not open the upgrade modal (the session-leak guard for threat T-03-01)
  artifacts:
    - .planning/phases/03-landing-page-go-pro-payment-entry-point/03-02-SUMMARY.md (checkpoint outcome record)
  key_links:
    - Runtime confirmation that goProIntentRef is consumed-and-cleared so a "Go Pro" intent never leaks into a subsequent plain login
---

<objective>
Human-verify that the `LandingPage.tsx` wiring from Plan 03-01 delivers all four Phase 3 success criteria at runtime — the three "Go Pro" flows (PAY-01, PAY-02, PAY-03) plus the plain-login non-regression guard (Success Criterion 4) — and that the pending-intent flag does not leak across sessions (threat T-03-01).

Purpose: The success criteria are fundamentally runtime behaviors (a modal opening, a redirect firing) that involve Firebase Auth and the Stripe test checkout; there is no frontend test framework, so a structured human check is the honest verification gate.
Output: A recorded pass/fail against each of the four numbered checks below.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-landing-page-go-pro-payment-entry-point/03-UI-SPEC.md

@frontend/src/pages/LandingPage.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all three "Go Pro" flows plus the plain-login non-regression guard</name>
  <files>frontend/src/pages/LandingPage.tsx (read-only — no changes; this task only observes runtime behavior)</files>
  <read_first>
    - frontend/src/pages/LandingPage.tsx (the version modified by Plan 03-01 — `handleClick()` and the `justLoggedIn` transition effect)
    - .planning/phases/03-landing-page-go-pro-payment-entry-point/03-UI-SPEC.md ("Interaction Contract" — the binding runtime behavior)
    - .planning/ROADMAP.md (Phase 3 "Success Criteria" — the four numbered outcomes)
  </read_first>
  <action>
    Run the app locally and walk through the four numbered checks in `<how-to-verify>` below, one at a time, recording pass/fail for each. Do not modify any source file in this task — it is a pure runtime verification gate. If any check fails, stop and report which check failed and the observed behavior so Plan 03-01 can be revised via gap closure.
  </action>
  <acceptance_criteria>
    - Check 1 (PAY-01): Clicking "Go Pro" starts the upgrade flow — the page does NOT merely scroll to the top and do nothing.
    - Check 2 (PAY-03): While logged in, clicking "Go Pro" opens the `UpgradeModal` immediately on the landing page with no navigation/redirect.
    - Check 3 (PAY-02): While logged out, clicking "Go Pro" opens the `LoginModal` showing "Log in to continue to Premium checkout."; after a successful sign-in the `UpgradeModal` opens automatically and the URL stays on `/` (the landing page), not `/roadmap`.
    - Check 4 (Success Criterion 4 + threat T-03-01): After signing out, clicking the nav "Log in" link and signing in redirects to `/roadmap` and does NOT open the `UpgradeModal` — even if a prior "Go Pro" login was started earlier in the same browser tab (the intent flag did not leak).
  </acceptance_criteria>
  <what-built>
    Plan 03-01 rewired the Pricing "Go Pro" CTA in `LandingPage.tsx`: a `data-goto-pro` delegated click branch opens the existing `UpgradeModal` for logged-in visitors and, for logged-out visitors, opens the existing `LoginModal` (message "Log in to continue to Premium checkout.") and then auto-opens the `UpgradeModal` on successful sign-in via a `goProIntentRef` consumed in the `justLoggedIn` transition effect. The plain nav "Log in" path still redirects to `/roadmap`. No components, routes, or dependencies were added.
  </what-built>
  <how-to-verify>
    Start the app: `cd backend && npm run dev` (port 3001) and `cd frontend && npm run dev` (port 5173). Open the landing page at `http://localhost:5173/` (or the LAN IP if testing the phone flow — not required here).

    1. **PAY-01 / PAY-03 (logged in):** Sign in first (nav "Log in"). Return to `/`. Scroll to the Pricing section and click **"Go Pro"** on the Pro card. EXPECT: the dark `UpgradeModal` (monthly/annual plan cards) opens over the landing page immediately; the URL stays `/`; the page does not jump to the top.
    2. **PAY-02 (logged out):** Sign out. On `/`, scroll to Pricing and click **"Go Pro"**. EXPECT: the `LoginModal` opens showing the text **"Log in to continue to Premium checkout."** Complete a sign-in (Google or email). EXPECT: the `LoginModal` closes and the `UpgradeModal` opens automatically; the URL remains `/` (you are NOT bounced to `/roadmap`).
    3. **Success Criterion 4 / leak guard:** Sign out again. This time click the nav **"Log in"** link (top-right, NOT "Go Pro") and sign in. EXPECT: you are redirected to `/roadmap` and NO `UpgradeModal` appears. Bonus leak check: sign out, click "Go Pro" to open the login modal, then close it WITHOUT signing in; now click the nav "Log in" and sign in — EXPECT `/roadmap` with no `UpgradeModal` (the stale Go Pro intent did not leak).
    4. Confirm the "Go Pro" button still looks identical (same gradient, same hover-lift) — only its click outcome changed.
  </how-to-verify>
  <verify>
    <human-check>All four checks above pass exactly as described, including the leak-guard bonus check in step 3.</human-check>
  </verify>
  <done>Human confirms all four checks pass: PAY-01 (CTA not a dead link), PAY-03 (logged-in instant modal), PAY-02 (logged-out login→auto-upgrade), and Success Criterion 4 (plain login → /roadmap, no upgrade modal, no intent leak).</done>
  <resume-signal>Type "approved" if all four checks pass, or describe which check failed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser React state ↔ Firebase Auth | This checkpoint exercises the logged-out→logged-in transition at runtime to confirm the intent flag is consumed correctly. Client-only; no untrusted input authored. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege (client logic) | `goProIntentRef` in `LandingPage.tsx` | low | mitigate | This checkpoint's Check 4 (incl. the leak-guard bonus) is the runtime verification that the intent flag is consumed-and-cleared and never leaks a "Go Pro" intent into a subsequent plain sign-in. |
| T-03-SC | Tampering (supply chain) | npm installs | low | accept | No packages installed; this is a verification-only plan (no `files_modified`). No install step exists. |

All threats low severity; none blocks.
</threat_model>

<verification>
- All four numbered checks pass at runtime.
- The "Go Pro" CTA is visually identical to before (gradient + hover-lift preserved); only the click outcome changed.
- No file is modified by this plan (`files_modified: []`); it is a pure verification gate.
</verification>

<success_criteria>
- Human confirms PAY-01, PAY-02, PAY-03, and Success Criterion 4 (plain-login non-regression) all hold at runtime, and that the pending-intent flag does not leak across sessions.
</success_criteria>

<output>
Create `.planning/phases/03-landing-page-go-pro-payment-entry-point/03-02-SUMMARY.md` when done, recording the pass/fail of each of the four checks.
</output>
